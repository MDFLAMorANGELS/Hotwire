<p style="color: green"><%= notice %></p>
<div class="container">
  <h1 >Liste</h1>

  <%= button_to "Créer un email",emails_path,data: {turbo_frame: "new_email"}%>

  <%= turbo_frame_tag "new_email" do%>

    <div id="emails">
      <% @emails.sort.reverse_each do |email| %>
      <div class="border border-solid p-2 m-2">
        <p><%= render email, email: email %></p>
        <p><%= render 'mailContent', email: email%></p>
      </div>
      <% end %>
    </div>
  <% end %>
</div>
