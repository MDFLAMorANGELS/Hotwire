<p style="color: green"><%= notice %></p>

<h1>Liste</h1>

<%= button_to "Créer un email",emails_path,data: {turbo_frame: "new_email"}%>

<%= turbo_frame_tag "new_email" do%>

  <div id="emails">
    <% @emails.each do |email| %>
      <%= render email %>
      <p>
        <%= link_to "Show this email", email%>
      </p>
    <% end %>
  </div>
<% end %>
